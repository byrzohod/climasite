/**
 * Bottom Navigation Component Public API
 * Mobile-only bottom navigation bar
 * 
 * @example
 * ```typescript
 * import { BottomNavComponent } from '@shared/components/bottom-nav';
 * 
 * // In your component imports
 * imports: [BottomNavComponent]
 * ```
 */

export { 
  BottomNavComponent,
  type BottomNavItem
} from './bottom-nav.component';
